"""Per-dataset split plans for STEP-028."""

from __future__ import annotations

from .core import SplitConfig

# output_id -> list of split configs
SPLIT_PLANS: dict[str, list[SplitConfig]] = {
    "DS0001_PP0001": [
        SplitConfig(
            split_id="SPLIT0001",
            output_id="DS0001_PP0001",
            dataset_id="DS0001",
            pipeline_id="PP0001",
            scheme="grouped_random",
            seed=42,
            ratios=(0.7, 0.15, 0.15),
            group_by=["identity", "generator"],
            notes="Primary train split; groups by identity+generator.",
        ),
        SplitConfig(
            split_id="SPLIT0002",
            output_id="DS0001_PP0001",
            dataset_id="DS0001",
            pipeline_id="PP0001",
            scheme="logo",
            seed=42,
            held_out_generators=["stable_diffusion"],
            train_val_ratio=(0.85, 0.15),
            group_by=["identity", "generator"],
            notes="Leave-one-generator-out (stable_diffusion held out for test).",
        ),
    ],
    "DS0002_PP0002": [
        SplitConfig(
            split_id="SPLIT0001",
            output_id="DS0002_PP0002",
            dataset_id="DS0002",
            pipeline_id="PP0002",
            scheme="grouped_random",
            seed=42,
            ratios=(0.7, 0.15, 0.15),
            group_by=["identity", "generator"],
            notes="DiFF eval pool internal grouped split (fake-only).",
        ),
        SplitConfig(
            split_id="SPLIT0002",
            output_id="DS0002_PP0002",
            dataset_id="DS0002",
            pipeline_id="PP0002",
            scheme="logo",
            seed=42,
            held_out_generators=["Midjourney"],
            train_val_ratio=(0.85, 0.15),
            group_by=["identity", "generator"],
            notes="LOGO: Midjourney held out for unseen-generator eval (E9).",
        ),
    ],
    "DS0003_PP0003": [
        SplitConfig(
            split_id="SPLIT0001",
            output_id="DS0003_PP0003",
            dataset_id="DS0003",
            pipeline_id="PP0003",
            scheme="official_holdout",
            seed=42,
            official_test_column="split",
            official_test_values=["valid"],
            train_val_ratio=(0.85, 0.15),
            group_by=["content_hash"],
            notes="Respect native train/valid; valid->test, train subsplit train/val by hash groups.",
        ),
        SplitConfig(
            split_id="SPLIT0002",
            output_id="DS0003_PP0003",
            dataset_id="DS0003",
            pipeline_id="PP0003",
            scheme="logo",
            seed=42,
            held_out_generators=["stylegan"],
            train_val_ratio=(0.85, 0.15),
            group_by=["content_hash"],
            notes="LOGO: stylegan fakes held out (native valid still test when present).",
        ),
    ],
    "DS0004_PP0006": [
        SplitConfig(
            split_id="SPLIT0001",
            output_id="DS0004_PP0006",
            dataset_id="DS0004",
            pipeline_id="PP0006",
            scheme="grouped_random",
            seed=42,
            ratios=(0.7, 0.15, 0.15),
            group_by=["identity", "generator"],
            notes="Synthbuster grouped by RAISE/synth stem identity.",
        ),
        SplitConfig(
            split_id="SPLIT0002",
            output_id="DS0004_PP0006",
            dataset_id="DS0004",
            pipeline_id="PP0006",
            scheme="logo",
            seed=42,
            held_out_generators=["dalle3"],
            train_val_ratio=(0.85, 0.15),
            group_by=["identity", "generator"],
            notes="LOGO: dalle3 generator held out for frequency supplementary eval.",
        ),
    ],
    "DS0005_PP0005": [
        SplitConfig(
            split_id="SPLIT0001",
            output_id="DS0005_PP0005",
            dataset_id="DS0005",
            pipeline_id="PP0005",
            scheme="official_holdout",
            seed=42,
            official_test_column="split",
            official_test_values=["val"],
            train_val_ratio=(0.85, 0.15),
            group_by=["identity"],
            notes="FairFace official val->test; train subsplit for tuning.",
        ),
        SplitConfig(
            split_id="SPLIT0002",
            output_id="DS0005_PP0005",
            dataset_id="DS0005",
            pipeline_id="PP0005",
            scheme="grouped_random",
            seed=42,
            ratios=(0.7, 0.15, 0.15),
            group_by=["identity"],
            notes="Alternate grouped random split for bias ablations.",
        ),
    ],
}
